function likelihood = gf_observ_unit_test(mu, sigma, lambda, T, alpha)
%N Mixture model

% likelihood = 0;
% for n = min(y):N_max
% 	% likelihood = likelihood + poisspdf(n, lambda) .* prod(arrayfun(@(yi) binopdf(yi,n,alpha),y));
% 	likelihood = likelihood + poisspdf(n, lambda) .* prod(binopdf(y,n,alpha));
% end



end

function gf_likelihood = gf_observ_likelihood()
	gf_likelihood 

end

